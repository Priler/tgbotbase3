version: "3.8"

services:
  bot:
    build: .
    container_name: tgbot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config.toml:/app/config.toml:ro
      - ./l10n:/app/l10n:ro
      - bot_data:/app/data
    environment:
      - TZ=UTC

volumes:
  bot_data:
